/**
 * LLM用語Wiki一括生成スクリプト
 * - 既存5ページをアプリ固有記述なしの汎用版に更新
 * - 新規用語ページを追加
 */
import path from "path";
import { PrismaClient } from "@prisma/client";
import { PrismaLibSql } from "@prisma/adapter-libsql";

const dbPath = path.resolve(process.cwd(), "dev.db");
const adapter = new PrismaLibSql({ url: `file:${dbPath}` });
const prisma = new PrismaClient({ adapter });

async function main() {
  const user = await prisma.user.upsert({
    where: { email: "dev@localhost" },
    update: {},
    create: { id: "dev-user", name: "Dev User", email: "dev@localhost" },
  });

  const tagNames = [
    "AI", "LLM", "エージェント", "RAG", "プロンプト", "マルチモーダル",
    "アーキテクチャ", "ファインチューニング", "評価", "2026",
  ];
  const tags = await Promise.all(
    tagNames.map((name) =>
      prisma.tag.upsert({ where: { name }, update: {}, create: { name } })
    )
  );
  const tagMap = Object.fromEntries(tags.map((t) => [t.name, t]));

  /** ページをupsert（既存なら内容を更新、なければ作成） */
  const upsertPage = async (slug: string, title: string, content: string, pageTags: string[]) => {
    const existing = await prisma.page.findUnique({ where: { slug } });

    if (existing) {
      await prisma.page.update({
        where: { slug },
        data: {
          title,
          content,
          tags: {
            deleteMany: {},
            create: pageTags.filter((n) => tagMap[n]).map((n) => ({
              tag: { connect: { id: tagMap[n].id } },
            })),
          },
        },
      });
      await prisma.revision.create({ data: { pageId: existing.id, content, authorId: user.id } });
      console.log(`  更新: ${title}`);
    } else {
      const page = await prisma.page.create({
        data: {
          slug,
          title,
          content,
          authorId: user.id,
          tags: {
            create: pageTags.filter((n) => tagMap[n]).map((n) => ({
              tag: { connect: { id: tagMap[n].id } },
            })),
          },
          revisions: { create: { content, authorId: user.id } },
        },
      });
      console.log(`  作成: ${title}`);
      return page;
    }
  };

  // ══════════════════════════════════════════════════════════════════
  // 既存ページ更新（アプリ固有記述を除去した汎用版）
  // ══════════════════════════════════════════════════════════════════

  await upsertPage("llm-overview", "LLM（大規模言語モデル）概要", `# LLM（大規模言語モデル）概要

大規模言語モデル（Large Language Model / LLM）は、数千億〜数兆パラメータを持つ深層学習モデル。自然言語のテキスト生成・理解・翻訳・コーディングなど幅広いタスクを実行できる。[[Transformer アーキテクチャ]]を基盤とし、大量のテキストデータで事前学習される。

## 主要モデル比較（2026年時点）

| モデル | 開発元 | 特徴 | コンテキスト |
|---|---|---|---|
| **GPT-5.4** | OpenAI | コンピューターユース（OSWorld 75%）、人間専門家水準 | 128K トークン |
| **Claude Opus 4.6** | Anthropic | SWE-bench 首位（Elo 1548）、コーディング最強クラス | 1M トークン |
| **Gemini 3.1 Pro** | Google | 16 ベンチマーク中 13 項目トップ、マルチモーダル最強 | 2M トークン |
| **Llama 4 Scout** | Meta | オープンウェイト、超長コンテキスト | 10M トークン |
| **DeepSeek V4** | DeepSeek | オープンウェイト、推論 1.8 倍高速、ネイティブマルチモーダル | 128K トークン |
| **Grok 4** | xAI | HLE ベンチマーク 50.7% でリード | — |

## LLM の動作原理

LLM は次のトークンを予測する確率モデルとして学習される。

1. **事前学習**：大量のテキストコーパスで自己教師あり学習（次単語予測）
2. **SFT（Supervised Fine-Tuning）**：人間が作成した指示応答ペアでファインチューニング
3. **[[RLHF]]**：人間のフィードバックを使った強化学習で有用性・安全性を向上

詳細は [[Transformer アーキテクチャ]] と [[ファインチューニング]] を参照。

## 2026 年の主要トレンド

### 推論モデルの普及
「考えてから答える」アーキテクチャ（OpenAI o3/o4、Claude Thinking 等）が全主要ラボで展開。複雑な問題でスコアが大幅に向上。

### コンテキストウィンドウの巨大化
- Llama 4 Scout：1,000 万トークン（書籍約 7,500 冊分）
- Claude Opus 4.6：100 万トークン
- Gemini 3.1 Pro：200 万トークン

詳細は [[コンテキストウィンドウ]] を参照。

### 特化の時代
2026 年は「単一モデルが全カテゴリを制覇しない」構造が定着。コーディング・マルチモーダル・推論速度・HLE スコアで各モデルが棲み分け。

### オープンソースの台頭
GLM-5.1（Zhipu AI）は NVIDIA ハードウェア非依存で Claude Opus 4.6 のコーディング性能 94.6% を達成。商用モデル独占の時代が終わりつつある。

## 主要評価ベンチマーク

| ベンチマーク | 評価内容 |
|---|---|
| **SWE-bench** | 実世界の GitHub イシュー解決（コーディング） |
| **GPQA Diamond** | 大学院レベルの科学知識 |
| **ARC-AGI-2** | 汎用推論能力 |
| **HLE（Humanity's Last Exam）** | 難問横断評価 |
| **OSWorld** | コンピューター操作タスク |

## 関連ページ

- [[Transformer アーキテクチャ]]
- [[トークンとトークナイザー]]
- [[コンテキストウィンドウ]]
- [[ファインチューニング]]
- [[RLHF]]
- [[ハルシネーション]]
- [[AIエージェント]]
- [[マルチモーダルAI]]
`, ["AI", "LLM", "2026"]);

  await upsertPage("ai-agents", "AIエージェント", `# AIエージェント

AIエージェントとは、LLM を「頭脳」として自律的に計画・ツール使用・反省のループを繰り返し、複雑なタスクを実行するシステム。単一の質問に答えるだけでなく、複数ステップにわたる作業を自律的にこなす。

## エージェントと通常の LLM の違い

| 観点 | 通常の LLM 呼び出し | AI エージェント |
|---|---|---|
| ステップ数 | 1 回の入出力 | 複数ステップのループ |
| ツール使用 | なし | 外部 API・検索・コード実行 |
| 自律性 | ユーザーが全手順を指定 | 自分で計画・判断 |
| 状態管理 | ステートレス | ステートフル（メモリあり） |

## 主要フレームワーク比較（2026 年）

| フレームワーク | 開発元 | 特徴 |
|---|---|---|
| **OpenAI Agents SDK** | OpenAI | 旧 Swarm を本番向けに刷新。GitHub スター 19,000 超 |
| **Google ADK** | Google | 4 言語対応。階層ツリー構造でルート→子エージェント委譲 |
| **Claude Agent SDK** | Anthropic | 旧「Claude Code SDK」改名。Claude 4.6 と並行リリース |
| **LangGraph** | LangChain | 月間検索 27,100 件でトップ人気。有向グラフによるステートフルワークフロー |
| **AutoGen / Semantic Kernel** | Microsoft | 統合され Agent Framework として一本化 |
| **CrewAI** | CrewAI | ロールベースのエージェント協調に強み |

## エージェント設計パターン

### Reflection（反省）
エージェントが自分の出力を評価・批評し、品質基準を満たすまで改善を繰り返す。

### Planning（計画）
与えられたタスクをサブタスクに分解し、順次実行する。ReAct（Reasoning + Acting）パターンが代表的。

\`\`\`
Thought: 次に何をすべきか考える
Action: ツールを呼び出す
Observation: 結果を観察する
→ ループ
\`\`\`

### Tool Use（ツール使用）
外部 API・検索エンジン・コード実行環境などを呼び出して情報収集や操作を行う。

### Multi-Agent（マルチエージェント）
専門化された複数エージェントが協調してタスクを分担。Orchestrator（指揮役）が Worker（実行役）に指示を出す構造が一般的。

## エージェント間通信プロトコル（2025 年標準化）

| プロトコル | 主体 | 役割 |
|---|---|---|
| **[[MCP（Model Context Protocol）]]** | Anthropic | AI モデルとツール・データソースを JSON-RPC で垂直統合 |
| **A2A（Agent-to-Agent）** | Google | エージェント同士が Agent Card と REST エンドポイントでタスクを水平委譲 |
| **ACP（Agent Communication Protocol）** | IBM → Linux Foundation | REST 標準。2025 年末に A2A と統合 |

## エンタープライズでの課題

2025〜2026 年にかけてエージェントの本番投入が増える中、「エージェント Ops」が重要課題として浮上：

- **可観測性**：何のツールを何回呼んだか、なぜその判断をしたかのトレーシング
- **コスト管理**：複数ステップのループが予想外のトークン消費を招く
- **ガバナンス**：エージェントが実行できるアクションの権限管理
- **再現性**：同じ入力に対して同じ出力が得られるかの保証

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[RAG（検索拡張生成）]]
- [[プロンプトエンジニアリング]]
- [[MCP（Model Context Protocol）]]
`, ["AI", "エージェント", "2026"]);

  await upsertPage("rag-retrieval-augmented-generation", "RAG（検索拡張生成）", `# RAG（検索拡張生成）

RAG（Retrieval-Augmented Generation）は、LLM の回答生成時に外部の知識ベースを動的に検索して文脈として注入する手法。モデルの学習データにない最新情報や組織固有の知識を扱えるようになる。[[ファインチューニング]]と並ぶ LLM のカスタマイズ手法だが、知識更新コストが低く実装しやすい。

## RAG vs ファインチューニング

| 観点 | RAG | ファインチューニング |
|---|---|---|
| 知識の更新 | ドキュメント差し替えのみ | 再学習が必要 |
| ハルシネーション | ソース引用で低減しやすい | モデル依存 |
| コスト | 比較的低い | GPU コストが高い |
| 適したユースケース | 最新情報・社内文書 | 特定ドメインのスタイル・能力 |

## 基本アーキテクチャ

\`\`\`
クエリ
  ↓
エンベディング変換（クエリをベクトル化）
  ↓
ベクトル検索（類似文書チャンクを取得）
  ↓
LLM にプロンプト ＋ チャンクを渡す
  ↓
回答生成（ソース引用付き）
\`\`\`

詳細は [[エンベディング（埋め込み）]] と [[ベクトルデータベース]] を参照。

## RAG の進化系（2025〜2026 年）

### Agentic RAG
従来の固定的な「1 回検索して終わり」から、自律的エージェントが複数回検索・計画・反省を行う動的パイプラインへ進化。ベースラインより一貫して **5〜13 ポイントの精度向上**。

### GraphRAG
コーパス上にエンティティ関係グラフを構築し、「このドキュメント全体に共通するテーマは？」といった**グローバルな問い**に対応。Microsoft のオープンソース実装が広く使われる。

### Multimodal RAG
テキストだけでなく、画像・動画・音声も検索対象とする RAG。フロンティアモデルの[[マルチモーダルAI]]化に伴い実用化が進んだ。

### Self-RAG
いつ検索するかを自己判断し、自分の出力を自己批評するよう訓練されたモデルを使うアプローチ。事実性と引用精度を向上させる。

## 実装上の重要概念

| 概念 | 説明 |
|---|---|
| チャンキング | 長文書を適切な単位に分割する処理。分割サイズが検索精度に大きく影響 |
| ハイブリッド検索 | セマンティック検索（ベクトル）＋キーワード検索（BM25）を組み合わせて精度を向上 |
| リランキング | 検索結果をクロスエンコーダーで再スコアリングし、LLM に渡すチャンクを選別 |
| コンテキスト圧縮 | 取得したチャンクから不要部分を除去し、LLM のコンテキスト使用量を削減 |

## 実績事例

**MARAUS**（大学入試カウンセリング用マルチエージェント RAG システム）
- 精度 92% 達成
- 幻覚率を 15% → **1.45%** に削減

## 関連ページ

- [[エンベディング（埋め込み）]]
- [[ベクトルデータベース]]
- [[ハルシネーション]]
- [[AIエージェント]]
- [[ファインチューニング]]
`, ["AI", "RAG", "LLM"]);

  await upsertPage("prompt-engineering", "プロンプトエンジニアリング", `# プロンプトエンジニアリング

プロンプトエンジニアリングとは、LLM から望ましい出力を得るためのプロンプト（入力テキスト）の設計・最適化技術。モデルの内部パラメータを変更せずに、入力の工夫だけで出力品質を大幅に向上させられる。

## 完了モデル vs 推論モデル（2025 年の大転換）

2025 年に「推論モデル」が普及し、プロンプトの書き方を根本的に見直す必要が生じた。

| 観点 | 完了モデル（GPT-4.1 等） | 推論モデル（o3・o4・Claude Thinking 等） |
|---|---|---|
| プロンプトスタイル | ステップバイステップで誘導 | 「何をすべきか」だけ明示、方法は任せる |
| CoT 指示 | 有効（「ステップで考えて」が効く） | 効果薄・むしろ邪魔になる場合あり |
| 詳細な手順指定 | 有効 | 自律性を阻害する可能性あり |
| 比喩 | 逐一指示が必要な新人 | 自分でアプローチを決める上級同僚 |

## 主要テクニック

### Zero-shot プロンプティング
例を一切与えず、タスクの説明だけで回答させる。GPT-3 以降の大規模モデルで有効。

\`\`\`
悪い名前をつけている変数を見つけて、より良い名前を提案してください。
コード: for i in range(len(a)): b += a[i]
\`\`\`

### Few-shot プロンプティング
いくつかの入出力例（ショット）を与えてパターンを学習させる。

\`\`\`
日本語 → 英語の翻訳:
猫 → cat
犬 → dog
桜 → ?
\`\`\`

### Chain-of-Thought（CoT）
複雑な問題を段階的なステップに分解させる。1,000 億パラメータ以上のモデルで特に効果的。

\`\`\`
この問題をステップごとに考えてください。
まず... 次に... 最後に...
\`\`\`

### Tree of Thoughts（ToT）
CoT の拡張。複数の推論パスを木構造で同時に生成・探索。戦略的計画が必要な問題に強い。

### Self-Consistency
同じ問いに対して複数の推論パスを生成し、最も一貫した答えを多数決で選択。ランダムエラーを低減。

### Role Prompting
「あなたは○○のエキスパートです」という役割設定。精度・推論・関連性が即座に向上する。

### System Prompt の活用
モデルの振る舞いの基本方針を設定する。出力フォーマット・禁止事項・ペルソナ等を指定。

## エージェント向けプロンプト設計

複数ステップ・ツール使用・ループを前提とする[[AIエージェント]]では以下を意識する：

- **タスクの明確な境界定義**：何をすべきで何をすべきでないかを明示
- **ツールの使用条件を明記**：どんな時にどのツールを使うかのルールを与える
- **終了条件の明示**：ループが無限に続かないよう完了基準を与える
- **エラー時の振る舞い指定**：想定外の状況でどう対処するかを記述

## プロンプトインジェクション攻撃

悪意ある入力でシステムプロンプトを上書きしたり、エージェントの行動を乗っ取ろうとする攻撃。セキュリティ上の重要な懸念事項。

## 2025〜2026 年のトレンド

- **自動プロンプト最適化**：AI 自身がプロンプトを最適化する Meta Prompting が実用化
- **マルチモーダルプロンプト**：テキスト・画像・音声を組み合わせたプロンプト設計が標準化
- **Constitutional AI**：倫理的制約をプロンプトに組み込む Anthropic 発の手法が業界標準化

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[AIエージェント]]
- [[RLHF]]
- [[LLM 評価指標]]
`, ["AI", "プロンプト", "LLM"]);

  await upsertPage("multimodal-ai", "マルチモーダルAI", `# マルチモーダルAI

マルチモーダル AI とは、テキストだけでなく画像・音声・動画・コードなど複数の「モダリティ（情報形式）」を理解・生成できる AI モデル。2025〜2026 年にフロンティアモデル全てがマルチモーダルを標準搭載し、業界の前提が変わった。

## 対応モダリティの種類

| モダリティ | 入力例 | 出力例 |
|---|---|---|
| テキスト | 質問・指示文 | 文章・コード |
| 画像 | 写真・スクリーンショット・図表 | 説明・分析 |
| 音声 | 会話・録音 | テキスト・音声応答 |
| 動画 | 会議録画・YouTube | 要約・Q&A |
| ドキュメント | PDF・スライド | 内容抽出・分析 |

## 主要モデルとマルチモーダル能力（2026 年）

### Gemini 3.1 Pro（Google）— マルチモーダル最強
- テキスト・画像・音声・動画・コードを**単一の会話でネイティブ処理**
- コンテキスト 200 万トークン（2 時間の動画を一括入力可能）
- 16 ベンチマーク中 13 項目でトップ（GPQA Diamond: 94.3%、ARC-AGI-2: 77.1%）
- 2026 年 2 月 19 日リリース

### GPT-5.4（OpenAI）
- テキスト・画像・音声のネイティブ処理（前処理ステップ不要）
- 音声応答 **320 ミリ秒** のリアルタイム音声会話
- コンピューターユース（OSWorld 75%）で人間専門家水準を超過

### Claude Opus 4.6（Anthropic）
- ビジョン対応（画像・ドキュメント解析）
- コンテキスト 100 万トークン
- コーディングとドキュメント処理の組み合わせに強み

### DeepSeek V4（オープンウェイト）
- テキスト・画像・音声・**動画**のネイティブマルチモーダル
- MODEL1 アーキテクチャでメモリ 40% 削減・推論 1.8 倍高速

## アーキテクチャの進化

### 初期：モダリティ別アダプター
テキストモデル（LLM）に画像エンコーダーを追加するアプローチ。処理は別々で、最後に統合。

### 現在：ネイティブマルチモーダル統合
全モダリティを**統一された内部表現**で処理するアーキテクチャが主流。モダリティをまたいだ推論精度が向上。

## 実用ユースケース

| ユースケース | 使用モダリティ |
|---|---|
| ドキュメント解析（PDF・スライド） | テキスト ＋ 画像 |
| リアルタイム音声アシスタント | 音声 |
| 動画コンテンツの要約・検索 | 動画 ＋ テキスト |
| コンピューター操作の自動化 | 画面キャプチャ ＋ テキスト |
| 医療画像診断支援 | 画像 ＋ テキスト |
| グラフ・チャートの解析 | 画像 → テキスト |

## オープンソースの急追

- **Qwen3-VL**（Alibaba）：コスト効率とマルチモーダル性能のバランス
- **GLM-4.6V**（Zhipu AI）：NVIDIA 非依存でフロンティアモデルに肉薄
- **DeepSeek V4**：ネイティブマルチモーダルでフルオープンウェイト

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[Transformer アーキテクチャ]]
- [[RAG（検索拡張生成）]]
`, ["AI", "マルチモーダル", "LLM", "2026"]);

  // ══════════════════════════════════════════════════════════════════
  // 新規用語ページ
  // ══════════════════════════════════════════════════════════════════

  await upsertPage("transformer-architecture", "Transformer アーキテクチャ", `# Transformer アーキテクチャ

Transformer は 2017 年に Google の論文「Attention Is All You Need」で提案されたニューラルネットワークアーキテクチャ。それ以前主流だった RNN・LSTM を置き換え、現在の[[LLM（大規模言語モデル）概要]]の基盤となっている。

## 登場の背景

RNN（再帰型ニューラルネット）は系列データを処理できるが、**長距離依存関係の学習が困難**で、**並列化できない**という問題があった。Transformer はこれを Self-Attention 機構で解決した。

## 主要構成要素

### Self-Attention（自己注意機構）
入力系列の各トークンが、他の全トークンとの関連度（注意重み）を計算し、文脈を考慮した表現を生成する。

\`\`\`
Query（クエリ）× Key（キー） → 注意スコア
注意スコア × Value（バリュー） → 出力表現
\`\`\`

「銀行に預けた」の「銀行」が「川岸」ではなく「金融機関」を意味すると判断できるのは Self-Attention のおかげ。

### Multi-Head Attention
Self-Attention を複数の「ヘッド」で並列実行。異なる種類の関係性（構文・意味・指示関係等）を同時に捉える。

### Feed-Forward Network
各位置に独立して適用される全結合層。特徴変換を担う。

### Positional Encoding
Transformer 自体は順序情報を持たないため、トークンの位置情報を埋め込みに加算して与える。

### Layer Normalization & Residual Connection
学習の安定化と勾配消失の防止。

## Encoder-Decoder vs Decoder-only

| アーキテクチャ | 代表モデル | 用途 |
|---|---|---|
| **Encoder-Decoder** | T5, BART | 翻訳・要約（入力→出力変換） |
| **Encoder-only** | BERT, RoBERTa | テキスト分類・埋め込み生成 |
| **Decoder-only** | GPT, Claude, Llama | テキスト生成・会話（現在の主流） |

現在の大規模モデルはほぼ全て Decoder-only。

## スケーリング則（Scaling Laws）

2020 年の OpenAI 論文で、モデルサイズ・データ量・計算量の増加に伴って性能が**べき乗則的**に向上することが示された。これが「大きいほど良い」という LLM 開発競争の理論的根拠となった。

## Transformer の限界と発展

| 課題 | 対応アーキテクチャ |
|---|---|
| コンテキスト長の制限（Self-Attention の O(n²) コスト） | Sparse Attention, Flash Attention, MoE |
| 推論コストの高さ | MoE（Mixture of Experts）、量子化 |
| 長い系列での位置エンコーディング精度 | RoPE（回転位置エンコーディング） |

## Mixture of Experts（MoE）

パラメータ数は大きいが、推論時には一部の「エキスパート」ネットワークしか活性化しないアーキテクチャ。GPT-4・Llama 4 等で採用。計算効率と性能を両立する。

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[トークンとトークナイザー]]
- [[エンベディング（埋め込み）]]
- [[ファインチューニング]]
`, ["AI", "LLM", "アーキテクチャ"]);

  await upsertPage("tokens-and-tokenizer", "トークンとトークナイザー", `# トークンとトークナイザー

LLM はテキストをそのまま処理するのではなく、**トークン**という単位に分割して扱う。この分割処理を行うのが**トークナイザー**。LLM の性能・コスト・制限を理解する上での基本概念。

## トークンとは

テキストをモデルが処理できる最小単位に分割したもの。単語・サブワード・文字・記号などが混在する。

| テキスト | トークン分割例（GPT 系） | トークン数 |
|---|---|---|
| \`Hello\` | \`Hello\` | 1 |
| \`unhappiness\` | \`un\` \`happiness\` | 2 |
| \`ChatGPT\` | \`Chat\` \`G\` \`PT\` | 3 |
| \`東京\` | \`東\` \`京\` | 2〜4（モデル依存） |

**目安**：英語は単語 1 つ ≈ 1〜2 トークン。日本語は文字 1 つ ≈ 1〜3 トークン（英語の約 2〜3 倍のトークン数になりやすい）。

## 主要トークナイザー

### BPE（Byte-Pair Encoding）
頻出する文字ペアを繰り返しマージしてサブワード単位を構築するアルゴリズム。GPT シリーズ（tiktoken）で採用。

### WordPiece
BPE の変形。BERT・BERT 派生モデルで採用。

### SentencePiece
言語非依存なサブワードトークナイザー。Llama・Gemma 等で採用。Unicode 全文字を対象にできる。

### tiktoken
OpenAI が開発した高速 BPE 実装。GPT-3.5 以降で使用。

## トークン数がコストに直結する理由

ほぼ全ての LLM API は**入力トークン数 ＋ 出力トークン数**で課金される。

\`\`\`
コスト = 入力トークン × 入力単価 + 出力トークン × 出力単価
\`\`\`

例：Claude Opus 4.6 の場合、出力トークン単価は入力の約 5 倍。長い出力は特にコストが高い。

## コンテキストウィンドウとの関係

モデルが一度に処理できるトークン数の上限が[[コンテキストウィンドウ]]。プロンプト ＋ 応答の合計がこの上限を超えると、古い内容から切り捨てられる。

## 日本語トークナイズの注意点

日本語は英語より多くのトークンを消費するため：
- 同じ内容でも英語より 2〜3 倍のコストになる場合がある
- コンテキストウィンドウを英語テキストより早く消費する
- 日本語最適化モデル（Llama 3 Japanese 等）ではトークン効率が改善されている

## 特殊トークン

| トークン | 役割 |
|---|---|
| \`<|begin_of_text|>\` | テキスト開始マーカー |
| \`<|end_of_text|>\` | テキスト終了マーカー |
| \`<|system|>\` | システムプロンプトの区切り |
| \`<|user|>\` \`<|assistant|>\` | チャットターンの区切り |

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[Transformer アーキテクチャ]]
- [[コンテキストウィンドウ]]
- [[エンベディング（埋め込み）]]
`, ["AI", "LLM"]);

  await upsertPage("context-window", "コンテキストウィンドウ", `# コンテキストウィンドウ

コンテキストウィンドウとは、LLM が一度の推論で参照できる**最大トークン数**。プロンプト・会話履歴・システム指示・出力の合計がこの上限に収まる必要がある。LLM 活用における最重要制約の一つ。

## 代表的なコンテキストウィンドウサイズ（2026 年）

| モデル | コンテキストサイズ | 相当する情報量の目安 |
|---|---|---|
| GPT-5.4 | 128K トークン | 書籍 1〜2 冊 |
| Claude Opus 4.6 | 1M トークン | 書籍約 750 冊 |
| Gemini 3.1 Pro | 2M トークン | 書籍約 1,500 冊 |
| Llama 4 Scout | 10M トークン | 書籍約 7,500 冊 |

## コンテキストに含まれるもの

\`\`\`
┌──────────────────────────────────────────┐
│  System Prompt（システム指示）           │
│  ─────────────────────────────────────── │
│  会話履歴（過去のやり取り）              │
│  ─────────────────────────────────────── │
│  現在のユーザー入力                      │
│  ─────────────────────────────────────── │
│  RAG で取得したドキュメント              │
│  ─────────────────────────────────────── │
│  モデルの出力（生成中）                  │
└──────────────────────────────────────────┘
         ↑ 合計がコンテキストウィンドウ以内
\`\`\`

## コンテキストウィンドウ拡大の恩恵

- **長文書の一括処理**：大規模コードベース全体・長編小説・年次報告書をそのまま入力
- **長い会話の維持**：会話履歴が切れずに一貫したコンテキストを保持
- **RAG なしでのドキュメント Q&A**：関連文書をそのまま詰め込む「long context」アプローチ

## ロストインザミドル問題

コンテキストが長くなると、**中間部分の情報を見落としやすくなる**現象（Lost in the Middle）。

- 先頭と末尾の情報は高い注意を受ける
- 重要情報はコンテキストの先頭か末尾に配置するのが有効
- 長コンテキストモデルでも完全には解決されていない

## コンテキスト管理の実装パターン

### Sliding Window（スライディングウィンドウ）
最新 N トークンのみを保持し、古い会話から切り捨てる。シンプルだが重要な過去の情報が失われる。

### Summary Memory（要約メモリ）
古い会話を要約して圧縮保存し、要約 ＋ 最新会話をコンテキストに入れる。

### [[RAG（検索拡張生成）]] との組み合わせ
膨大な情報をベクトルDBに格納し、クエリに関連する部分だけを動的にコンテキストに注入する。最も一般的な長期記憶の実装方法。

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[トークンとトークナイザー]]
- [[RAG（検索拡張生成）]]
- [[AIエージェント]]
`, ["AI", "LLM"]);

  await upsertPage("embeddings", "エンベディング（埋め込み）", `# エンベディング（埋め込み）

エンベディング（Embedding / 埋め込み）とは、テキスト・画像・音声などのデータを**高次元の数値ベクトル**に変換する表現学習技術。意味的に類似したデータは近いベクトルになるという性質を持ち、[[RAG（検索拡張生成）]]・検索・推薦・分類など幅広い用途で使われる。

## 直感的な理解

\`\`\`
「猫」   → [0.2, 0.8, 0.1, ...]   ┐
「ネコ」 → [0.21, 0.79, 0.11, ...] ┤ 近い（コサイン類似度が高い）
「犬」   → [0.3, 0.4, 0.6, ...]    ┘ やや近い（ペット）
「自動車」→ [0.9, 0.1, 0.2, ...]      遠い
\`\`\`

## 代表的なエンベディングモデル（2026 年）

| モデル | 開発元 | 次元数 | 特徴 |
|---|---|---|---|
| **text-embedding-3-large** | OpenAI | 3,072 | 高精度。RAG・検索用途に広く使用 |
| **text-embedding-3-small** | OpenAI | 1,536 | コスト効率重視 |
| **Cohere Embed v4** | Cohere | 1,024 | マルチモーダル対応（テキスト＋画像） |
| **GTE-Qwen3-7B** | Alibaba | 4,096 | オープンウェイト。MTEB ランキング上位 |
| **all-MiniLM-L6-v2** | Sentence-Transformers | 384 | 軽量・高速。ローカル実行向け |

## エンベディングの生成

\`\`\`python
import openai

response = openai.embeddings.create(
    model="text-embedding-3-large",
    input="大規模言語モデルの仕組みを教えてください"
)
vector = response.data[0].embedding  # 3072 次元のリスト
\`\`\`

## 類似度の計算

### コサイン類似度（最も一般的）
ベクトルの向き（角度）で類似度を測る。値域 -1〜1、1 に近いほど類似。

\`\`\`
similarity = cos(θ) = (A · B) / (|A| × |B|)
\`\`\`

### 内積（Dot Product）
ベクトルが正規化済みの場合はコサイン類似度と等価。計算が高速なため近似検索で使われる。

## RAG における役割

1. **インデックス時**：ドキュメントをチャンク分割 → 各チャンクをベクトル化 → [[ベクトルデータベース]]に保存
2. **クエリ時**：ユーザーの質問をベクトル化 → DB で近傍検索 → 類似チャンクを取得

## マルチモーダルエンベディング

テキストと画像を同一ベクトル空間に埋め込む技術（CLIP 等）。「白い犬」というテキストと犬の画像が近いベクトルになる。

## Fine-tuning によるカスタマイズ

汎用エンベディングモデルを特定ドメインのデータでファインチューニングすることで、ドメイン固有の類似概念をより正確に表現できる。

## 関連ページ

- [[RAG（検索拡張生成）]]
- [[ベクトルデータベース]]
- [[Transformer アーキテクチャ]]
- [[ファインチューニング]]
`, ["AI", "LLM", "RAG"]);

  await upsertPage("vector-database", "ベクトルデータベース", `# ベクトルデータベース

ベクトルデータベース（Vector Database）は、[[エンベディング（埋め込み）]]で生成された高次元ベクトルを効率的に格納・検索するためのデータベース。「意味的に類似したデータを素早く見つける」近傍探索に特化している。

## 従来のデータベースとの違い

| 観点 | RDB（SQL） | ベクトルDB |
|---|---|---|
| 検索の基準 | 完全一致・範囲 | 意味的類似度（コサイン距離等） |
| データ型 | 構造化データ | 高次元浮動小数点ベクトル |
| クエリ例 | \`WHERE name = 'foo'\` | 「この文章と似ているドキュメントを返して」 |
| 用途 | トランザクション・集計 | RAG・推薦・類似検索 |

## 近傍探索アルゴリズム

### HNSW（Hierarchical Navigable Small World）
グラフ構造を使った近似最近傍探索。速度と精度のバランスが優れており、多くの DB で採用。

### IVF（Inverted File Index）
ベクトルをクラスタリングして索引化。大規模データセットに向く。

### FAISS
Meta が開発した近似最近傍探索ライブラリ。多くのベクトル DB の内部で使用される。

## 主要ベクトルデータベース（2026 年）

| DB | 特徴 | ホスティング |
|---|---|---|
| **Pinecone** | マネージドサービス特化。スケーラビリティ高 | クラウドのみ |
| **Weaviate** | オープンソース。GraphQL API 対応 | セルフホスト / クラウド |
| **Qdrant** | Rust 実装で高速。フィルタリング機能が豊富 | セルフホスト / クラウド |
| **Chroma** | 軽量・開発用途向け。Python ネイティブ | セルフホスト |
| **pgvector** | PostgreSQL 拡張。既存 PG 環境に追加可能 | セルフホスト |
| **Azure AI Search** | フルマネージド。ハイブリッド検索 ＋ 意味検索対応 | Azure |

## ハイブリッド検索

ベクトル検索（セマンティック）とキーワード検索（BM25）を組み合わせる手法。

\`\`\`
最終スコア = α × セマンティックスコア + (1-α) × BM25 スコア
\`\`\`

- **ベクトル検索の強み**：意味が似ているが表現が違うドキュメントを発見（同義語・言い換え）
- **キーワード検索の強み**：固有名詞・コード・モデル名等の完全一致

## メタデータフィルタリング

ベクトル類似度検索にメタデータ条件を組み合わせる。

\`\`\`
「機械学習について」という意味で検索 AND 作成日 > 2025-01-01 AND タグ = "入門"
\`\`\`

## RAG パイプラインでの使い方

1. ドキュメントを[[エンベディング（埋め込み）]]モデルでベクトル化
2. チャンク ＋ ベクトル ＋ メタデータをベクトル DB に保存
3. クエリをベクトル化して近傍検索
4. 取得した上位 K チャンクを LLM のコンテキストに渡す

## 関連ページ

- [[エンベディング（埋め込み）]]
- [[RAG（検索拡張生成）]]
- [[LLM（大規模言語モデル）概要]]
`, ["AI", "LLM", "RAG"]);

  await upsertPage("fine-tuning", "ファインチューニング", `# ファインチューニング

ファインチューニング（Fine-Tuning）は、事前学習済みの LLM を特定のタスク・ドメイン・スタイルに適応させるための追加学習技術。事前学習で獲得した汎用的な知識を保持しつつ、目的に合った振る舞いを獲得させる。

## ファインチューニング vs プロンプトエンジニアリング vs RAG

| 手法 | コスト | 知識更新 | 適した用途 |
|---|---|---|---|
| **プロンプトエンジニアリング** | 低 | 即時（プロンプト変更） | 汎用タスク、プロトタイプ |
| **[[RAG（検索拡張生成）]]** | 中 | 即時（文書追加） | 最新情報・大量文書 |
| **ファインチューニング** | 高 | 再学習が必要 | 特定スタイル・ドメイン特化能力 |

## 主要な手法

### Full Fine-Tuning（フルファインチューニング）
モデルの全パラメータを更新する。性能は最も高いが GPU コストが非常に高く、数百億パラメータのモデルでは現実的でない。

### LoRA（Low-Rank Adaptation）
元のモデルのパラメータを固定し、低ランク行列を追加して学習する手法。

\`\`\`
W' = W + ΔW = W + B×A
（W: 元の重み、A・B: 低ランク行列）
\`\`\`

- 学習パラメータ数を **99% 以上削減**可能
- VRAM 消費が大幅に減少
- 現在の主流 PEFT（Parameter-Efficient Fine-Tuning）手法

### QLoRA
LoRA に 4-bit 量子化を組み合わせた手法。消費者向け GPU（24GB VRAM 程度）で 70B 規模のモデルをファインチューニング可能にした。

### Instruction Tuning（指示チューニング）
「指示 → 応答」形式のデータセットでファインチューニングし、指示に従う能力を向上させる。ChatGPT・Llama-Instruct 等の基盤技術。

### DPO（Direct Preference Optimization）
[[RLHF]] の代替手法。人間の好みデータ（良い応答・悪い応答のペア）から直接最適化する。[[RLHF]] より実装がシンプル。

## 学習データの形式（チャットテンプレート）

\`\`\`json
{
  "messages": [
    {"role": "system",    "content": "あなたは法律の専門家です。"},
    {"role": "user",      "content": "契約書の確認をお願いします。"},
    {"role": "assistant", "content": "承知しました。どのような契約書でしょうか？"}
  ]
}
\`\`\`

## ファインチューニングが有効なケース

- 特定の出力フォーマット（JSON スキーマ等）を常に守らせたい
- 社内専門用語・業界知識を深く理解させたい
- 特定のトーン・スタイルで一貫して返答させたい
- プロンプトで毎回説明するには長すぎる背景知識がある

## 評価と注意点

- **カタストロフィック・フォーゲッティング**：ファインチューニングで元の汎用能力が失われる現象
- **過学習**：学習データが少ない場合、汎化性能が落ちる
- **データ品質が最重要**：1,000 件の高品質データ > 10,000 件の低品質データ

## 関連ページ

- [[RLHF]]
- [[LLM（大規模言語モデル）概要]]
- [[RAG（検索拡張生成）]]
- [[プロンプトエンジニアリング]]
- [[LLM 評価指標]]
`, ["AI", "LLM", "ファインチューニング"]);

  await upsertPage("rlhf", "RLHF", `# RLHF

RLHF（Reinforcement Learning from Human Feedback / 人間のフィードバックによる強化学習）は、LLM を人間の価値観・好みに合わせて調整するための技術。ChatGPT・Claude・Gemini 等が「有用で、無害で、正直な」応答をするようになった最大の要因。

## RLHF が解決する問題

事前学習 ＋ SFT だけでは以下の問題が残る：
- 有害・差別的なコンテンツを生成してしまう
- ユーザーが望む応答スタイルと乖離する
- 嘘をついてでも「それらしい」答えを返す

RLHF は人間の好みを報酬シグナルとして使い、これらを改善する。

## 3 ステップのプロセス

### Step 1：SFT（Supervised Fine-Tuning）
人間が作成した「良い応答例」でモデルをファインチューニング。指示に従う基本的な能力を獲得させる。

### Step 2：報酬モデル（Reward Model）の学習
人間のアノテーターが複数の応答を比較評価（「A と B ならどちらが良い？」）。このデータで**報酬モデル**を学習させる。

\`\`\`
入力: (プロンプト, 応答A, 応答B)
出力: Aの方が良い確率
       ↓
報酬モデル: 任意の応答に対してスコアを予測できるモデル
\`\`\`

### Step 3：PPO による強化学習
報酬モデルのスコアを報酬として、PPO（Proximal Policy Optimization）アルゴリズムでモデルを最適化。

\`\`\`
LLM の出力 → 報酬モデルがスコア付け → スコアが高くなるようパラメータ更新
\`\`\`

## 課題

- **報酬ハッキング**：報酬モデルの抜け穴をついた高スコアだが実際には低品質な応答を生成する
- **コスト**：人間のアノテーションに膨大な費用と時間がかかる
- **スケーラビリティ**：人間より賢いモデルの応答を人間が適切に評価できるか？（スーパーアライメント問題）

## RLHF の代替・発展手法

### DPO（Direct Preference Optimization）
報酬モデルを使わず、好みデータから直接最適化。Llama 3・Mistral 等で広く採用。

### Constitutional AI（CAI）
Anthropic が開発。「憲法」（原則リスト）に基づき AI が自己評価・修正するアプローチ。人間のアノテーション量を削減。

### RLAIF（Reinforcement Learning from AI Feedback）
人間の代わりに AI が応答を評価。コスト削減と大規模化が可能だが、AI の偏りが引き継がれるリスク。

### GRPO（Group Relative Policy Optimization）
DeepSeek が採用。グループ内の応答を相対比較することで PPO より効率的に最適化。

## アライメントとの関係

RLHF は「AI アライメント」（AI を人間の意図に沿わせる）の現時点で最も実用的なアプローチ。ただし、以下の課題は未解決：

- **スペック・ゲーミング**：指標を満たすが本来の目標を達成しない行動
- **分布シフト**：学習時と異なるシチュエーションへの対応
- **価値の多様性**：誰の価値観を「正解」とするか

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[ファインチューニング]]
- [[プロンプトエンジニアリング]]
- [[LLM 評価指標]]
`, ["AI", "LLM", "ファインチューニング"]);

  await upsertPage("hallucination", "ハルシネーション", `# ハルシネーション

ハルシネーション（Hallucination / 幻覚）とは、LLM が事実と異なる情報を、あたかも確信を持っているかのように生成してしまう現象。「存在しない論文を引用する」「実在しない人物の経歴を作り上げる」「誤った数値を断言する」などが典型例。

## なぜハルシネーションが起きるか

LLM は本質的に「次のトークンを予測する確率モデル」であり、「事実を参照するデータベース」ではない。

- **統計的なパターン補完**：文脈から「それらしい」続きを生成する
- **訓練データの誤情報**：学習データに含まれる誤情報をそのまま学習
- **知識カットオフ**：学習データ以降の最新情報を持たない
- **過信バイアス**：不確実な場合でも断定的に答える傾向

## ハルシネーションの種類

### 事実的ハルシネーション
正しくない具体的情報（日付・人名・数値・引用等）を生成する。

\`\`\`
悪例: 「Einstein は 1905 年にノーベル物理学賞を受賞しました」
      （実際は 1921 年）
\`\`\`

### 忠実性ハルシネーション
入力された文書・コンテキストに存在しない情報を追加して要約・回答する。RAG システムで特に問題になる。

### 推論エラー
論理的に誤った推論ステップから誤った結論を導く。

## 対策手法

### [[RAG（検索拡張生成）]]
外部の信頼できる情報源を参照させ、それに基づいた回答を生成させる。回答にソース引用を付けることで検証可能にする。

### Self-Consistency（自己一貫性）
同じ質問に複数回回答させ、一致する答えを採用する。ランダムな誤りを低減できる。

### Chain-of-Thought プロンプティング
思考過程を明示させることで、誤った飛躍を発見しやすくする。

### Grounding（グラウンディング）
回答を特定のドキュメントや事実に「紐付ける」制約を与える。「提供された文書のみに基づいて答えてください」等。

### 不確実性の表明
「わかりません」「確認が必要です」と答えられるよう指示する。

### 二段階検証
生成した回答を別のプロンプトで事実確認させる。

## ハルシネーション評価指標

| 指標 | 内容 |
|---|---|
| **ROUGE** | 参照テキストとの N-gram 一致率 |
| **FactScore** | 生成文中の各事実主張の検証スコア |
| **TruthfulQA** | 一般的な誤解・神話に関する質問への回答精度 |

## モデル別の特性

- **推論モデル**（o3・Claude Thinking 等）：段階的思考によりハルシネーションが比較的少ない
- **高温度（Temperature）設定**：創造性は上がるがハルシネーションも増える
- **モデルサイズ**：一般的に大きいモデルほど事実精度が高い傾向

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[RAG（検索拡張生成）]]
- [[プロンプトエンジニアリング]]
- [[LLM 評価指標]]
`, ["AI", "LLM", "評価"]);

  await upsertPage("llm-evaluation", "LLM 評価指標", `# LLM 評価指標

LLM の性能を定量的に測るためのベンチマーク・指標の体系。「どのモデルが優れているか」を客観的に比較するために不可欠だが、ベンチマークの選択・解釈には注意が必要。

## なぜ LLM の評価が難しいか

- 出力が自由形式テキストのため、単純な正誤判定が難しい
- タスクの多様性（コーディング・推論・創作・会話等）を網羅するのが困難
- **ベンチマーク汚染**：学習データにテスト問題が含まれる可能性
- 評価者の主観（人間評価の場合）

## 主要ベンチマーク（2026 年）

### 推論・知識

| ベンチマーク | 内容 | 満点 |
|---|---|---|
| **MMLU** | 57 分野の多肢選択問題（高校〜大学院レベル） | 100% |
| **GPQA Diamond** | 大学院レベルの科学知識（専門家でも 65%程度） | 100% |
| **ARC-AGI-2** | 視覚的パターン認識・汎用推論 | 100% |
| **HLE（Humanity's Last Exam）** | 各分野の難問横断評価 | 100% |
| **DROP** | 離散推論（計算・比較を含む読解） | F1 スコア |

### コーディング

| ベンチマーク | 内容 |
|---|---|
| **HumanEval** | Python コーディング問題（164 問） |
| **SWE-bench** | 実世界の GitHub イシュー解決（難易度高） |
| **SWE-bench Verified** | SWE-bench の人間検証済みサブセット |

### 会話・指示追従

| ベンチマーク | 内容 |
|---|---|
| **MT-Bench** | マルチターン会話の品質評価（GPT-4 が審判） |
| **AlpacaEval** | 指示追従の相対比較評価 |
| **Chatbot Arena / LMSYS** | 人間がブラインド比較投票する Elo レーティング |

### 安全性・有害性

| ベンチマーク | 内容 |
|---|---|
| **TruthfulQA** | 一般的な誤解・神話への回答精度 |
| **BBQ** | バイアス測定（人口統計グループへの偏り） |

## 評価スコア比較（2026 年主要モデル）

| モデル | GPQA Diamond | SWE-bench Verified | Chatbot Arena Elo |
|---|---|---|---|
| Claude Opus 4.6 | ~85% | ~70% | 1548 |
| Gemini 3.1 Pro | 94.3% | 80.6% | — |
| GPT-5.4 | ~88% | ~75% | — |
| Grok 4 | — | — | HLE 50.7% |

## 評価手法の種類

### 自動評価
- **参照ベース**：模範解答との比較（ROUGE・BLEU・F1）
- **LLM as Judge**：GPT-4 等が出力を評価（MT-Bench 等）
- **コード実行**：生成コードを実際に実行してテスト（HumanEval 等）

### 人間評価
- **絶対評価**：各応答を独立して 1〜5 点で評価
- **相対評価**：A vs B のどちらが良いかを比較（Chatbot Arena 等）

## ベンチマーク解釈の注意点

- **ベンチマーク汚染**：学習データにテスト問題が含まれていると不正確に高スコアになる
- **サチュレーション**：優秀なモデルが 90%+ を達成すると差がつかなくなる（ARC-Easy 等）
- **実タスクとのギャップ**：ベンチマーク高スコア ≠ 実業務での高性能

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[ハルシネーション]]
- [[ファインチューニング]]
- [[RLHF]]
`, ["AI", "LLM", "評価"]);

  await upsertPage("mcp-model-context-protocol", "MCP（Model Context Protocol）", `# MCP（Model Context Protocol）

MCP（Model Context Protocol）は、Anthropic が 2024 年 11 月に公開したオープンプロトコル。AI モデル（LLM）と外部ツール・データソースを標準化された方法で接続するための仕様。「AI のための USB-C」とも表現される。

## 登場の背景

AI エージェントが外部ツールを呼び出す場合、これまでは各ツールごとに独自のインテグレーションを実装する必要があった。

\`\`\`
従来: LLM → [カスタムコード A] → データベース
      LLM → [カスタムコード B] → GitHub
      LLM → [カスタムコード C] → Slack
         （M×N 問題：M モデル × N ツールで実装が爆発）

MCP:  LLM → [MCP クライアント] → MCP プロトコル → [MCP サーバー]
         （標準化により M+N 問題に削減）
\`\`\`

## アーキテクチャ

### MCP ホスト
Claude Desktop・Cursor・VS Code 等の AI アプリケーション。MCP クライアントを内蔵する。

### MCP クライアント
ホスト内に存在し、MCP サーバーとの通信を管理する。

### MCP サーバー
特定のツール・データソースへのアクセスを提供する軽量プログラム。

\`\`\`
Claude → MCP クライアント → JSON-RPC 2.0 → MCP サーバー → ファイルシステム
                                          → MCP サーバー → GitHub API
                                          → MCP サーバー → PostgreSQL
\`\`\`

## MCP サーバーが提供できるもの

### Tools（ツール）
モデルが呼び出せる関数。副作用あり。

\`\`\`json
{
  "name": "create_file",
  "description": "ファイルを作成する",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {"type": "string"},
      "content": {"type": "string"}
    }
  }
}
\`\`\`

### Resources（リソース）
モデルが読み取れるデータ（ファイル・DB レコード・API レスポンス等）。

### Prompts（プロンプトテンプレート）
再利用可能なプロンプトパターン。

## 公式 MCP サーバーの例

| サーバー | 機能 |
|---|---|
| **filesystem** | ローカルファイルの読み書き |
| **github** | リポジトリ・イシュー・PR の操作 |
| **google-drive** | Google Drive ファイルのアクセス |
| **slack** | チャンネル読み取り・メッセージ送信 |
| **postgres** | PostgreSQL クエリ実行 |
| **brave-search** | Web 検索 |

## 採用状況（2025〜2026 年）

- **Claude Desktop**：2024 年 11 月から公式サポート
- **Cursor・VS Code Copilot**：対応済み
- **OpenAI**：2025 年 3 月に MCP サポートを発表（Agents SDK に統合）
- **Google ADK**：MCP ツール使用をサポート
- 公式・コミュニティ合わせて **5,000 以上の MCP サーバー**が公開（2025 年末時点）

## [[AIエージェント]]との関係

MCP は[[AIエージェント]]のツール層を標準化する。エージェントフレームワーク（LangGraph・AutoGen 等）は MCP サーバーをツールとしてネイティブに利用できる。

## セキュリティ考慮事項

- **Tool Poisoning**：悪意ある MCP サーバーが指示を隠し LLM の行動を操作する攻撃
- **プロンプトインジェクション**：リソース内のコンテンツが MCP 経由で LLM に読み込まれる際の攻撃
- 信頼できるサーバーのみを登録・使用することが重要

## 関連ページ

- [[AIエージェント]]
- [[LLM（大規模言語モデル）概要]]
- [[プロンプトエンジニアリング]]
`, ["AI", "エージェント", "2026"]);

  console.log("\n✓ LLM 用語 Wiki 生成完了");
}

main()
  .catch((e) => { console.error(e); process.exit(1); })
  .finally(() => prisma.$disconnect());
