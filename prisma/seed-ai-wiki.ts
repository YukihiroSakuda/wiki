import path from "path";
import { PrismaClient } from "@prisma/client";
import { PrismaLibSql } from "@prisma/adapter-libsql";

const dbPath = path.resolve(process.cwd(), "dev.db");
const adapter = new PrismaLibSql({ url: `file:${dbPath}` });
const prisma = new PrismaClient({ adapter });

async function main() {
  const user = await prisma.user.upsert({
    where: { email: "dev@localhost" },
    update: {},
    create: {
      id: "dev-user",
      name: "Dev User",
      email: "dev@localhost",
      accentColor: "blue",
      theme: "light",
    },
  });

  const tagNames = ["AI", "LLM", "エージェント", "RAG", "プロンプト", "マルチモーダル", "2026"];
  const tags = await Promise.all(
    tagNames.map((name) =>
      prisma.tag.upsert({ where: { name }, update: {}, create: { name } })
    )
  );
  const tagMap = Object.fromEntries(tags.map((t) => [t.name, t]));

  const createPage = async (slug: string, title: string, content: string, pageTags: string[]) => {
    const existing = await prisma.page.findUnique({ where: { slug } });
    if (existing) {
      console.log(`  スキップ（既存）: ${title}`);
      return existing;
    }

    const page = await prisma.page.create({
      data: {
        slug,
        title,
        content,
        authorId: user.id,
        tags: {
          create: pageTags
            .filter((n) => tagMap[n])
            .map((n) => ({ tag: { connect: { id: tagMap[n].id } } })),
        },
        revisions: {
          create: { content, authorId: user.id },
        },
      },
    });

    console.log(`  作成: ${title}`);
    return page;
  };

  // ─── 1. LLM概要 ──────────────────────────────────────────────────────────────
  await createPage(
    "llm-overview",
    "LLM（大規模言語モデル）概要",
    `# LLM（大規模言語モデル）概要

大規模言語モデル（Large Language Model / LLM）は、数千億〜数兆パラメータを持つ深層学習モデルで、自然言語のテキスト生成・理解・翻訳・コーディングなど幅広いタスクを実行できる。

## 主要モデル比較（2026年時点）

| モデル | 開発元 | 特徴 | コンテキスト |
|---|---|---|---|
| **GPT-5.4** | OpenAI | コンピューターユース（OSWorld 75%）、人間専門家水準を超過 | 128,000トークン |
| **Claude Opus 4.6** | Anthropic | SWE-bench首位（Arena Elo 1548）、コーディング最強クラス | 1,000,000トークン |
| **Gemini 3.1 Pro** | Google | 16ベンチマーク中13項目トップ、マルチモーダル最強 | 2,000,000トークン |
| **Llama 4 Scout** | Meta | オープンウェイト、コンテキスト1000万トークン | 10,000,000トークン |
| **DeepSeek V4** | DeepSeek | オープンウェイト、推論1.8倍高速、ネイティブマルチモーダル | 128,000トークン |
| **Grok 4** | xAI | HLEベンチマーク50.7%でリード | - |

## 2026年の主要トレンド

### 推論モデルの普及
「考えてから答える」アーキテクチャ（OpenAI o3/o4、Claude Thinking等）が全主要ラボで展開。複雑な問題でスコアが大幅に向上した一方、単純タスクではコストが高い点が課題。

### コンテキストウィンドウの巨大化
- Llama 4 Scout：1000万トークン（書籍約7,500冊分）
- Claude Opus 4.6：100万トークン（大規模コードベース全体を一括入力可能）
- Gemini 3.1 Pro：200万トークン（2時間の動画を一括処理）

### 特化の時代
2026年は「単一モデルが全カテゴリを制覇しない」構造が定着。
- コーディング → Claude Opus 4.6
- マルチモーダル → Gemini 3.1 Pro
- 推論速度 → Llama 4 Scout
- HLEベンチマーク → Grok 4

### オープンソースの台頭
GLM-5.1（Zhipu AI）はNVIDIAハードウェア非依存でClaude Opus 4.6のコーディング性能94.6%を達成。商用モデル独占の時代が終わりつつある。

## 評価ベンチマーク

| ベンチマーク | 評価内容 |
|---|---|
| **SWE-bench** | 実世界のGitHubイシュー解決（コーディング） |
| **GPQA Diamond** | 大学院レベルの科学知識 |
| **ARC-AGI-2** | 汎用推論能力 |
| **HLE（Humanity's Last Exam）** | 難問横断評価 |
| **OSWorld** | コンピューター操作タスク |

## 関連ページ

- [[AIエージェント]]
- [[マルチモーダルAI]]
- [[プロンプトエンジニアリング]]
`,
    ["AI", "LLM", "2026"]
  );

  // ─── 2. AIエージェント ───────────────────────────────────────────────────────
  await createPage(
    "ai-agents",
    "AIエージェント",
    `# AIエージェント

AIエージェントとは、LLMを「頭脳」として自律的に計画・ツール使用・反省のループを繰り返し、複雑なタスクを実行するシステム。単一の質問に答えるだけでなく、複数ステップにわたる作業を自律的にこなす。

## 主要フレームワーク比較（2026年）

| フレームワーク | 開発元 | 特徴 |
|---|---|---|
| **OpenAI Agents SDK** | OpenAI | 旧Swarmを本番向けに刷新。GitHubスター19,000超、月間1030万DL |
| **Google ADK** | Google | 4言語対応。階層的ツリー構造でルート→子エージェント委譲 |
| **Claude Agent SDK** | Anthropic | 旧「Claude Code SDK」改名。Claude 4.6と並行リリース |
| **LangGraph** | LangChain | 月間検索27,100件でトップ人気。有向グラフによるステートフルワークフロー |
| **AutoGen / Semantic Kernel** | Microsoft | 統合されAgent Frameworkとして一本化 |
| **CrewAI** | CrewAI | ロールベースのエージェント協調に強み |

## エージェント設計パターン

### Reflection（反省）
エージェントが自分の出力を評価・批評し、品質が基準を満たすまで改善を繰り返す。

### Planning（計画）
与えられたタスクをサブタスクに分解し、順次実行する。ReAct（Reasoning + Acting）パターンが代表的。

### Tool Use（ツール使用）
外部API・検索エンジン・コード実行環境などを呼び出して情報収集や操作を行う。

### Multi-Agent（マルチエージェント）
専門化された複数エージェントが協調してタスクを分担する。Orchestrator（指揮役）がWorker（実行役）に指示を出す構造が一般的。

## エージェント間通信プロトコル（2025年標準化）

2025年はエージェント間通信の標準化が急速に進んだ年。

| プロトコル | 主体 | 役割 |
|---|---|---|
| **MCP（Model Context Protocol）** | Anthropic | AIモデルとツール・データソースをJSON-RPCで垂直統合 |
| **A2A（Agent-to-Agent）** | Google | エージェント同士がAgent CardとRESTエンドポイントでタスクを水平委譲 |
| **ACP（Agent Communication Protocol）** | IBM → Linux Foundation | RESTs標準。2025年末にA2Aと統合 |

## エンタープライズでの課題

2025〜2026年にかけてエージェントの本番投入が増える中、以下の「エージェントOps」が重要課題として浮上：

- **可観測性**：何のツールを何回呼んだか、なぜその判断をしたかのトレーシング
- **コスト管理**：複数ステップのループが予想外のトークン消費を招く
- **ガバナンス**：エージェントが実行できるアクションの権限管理
- **再現性**：同じ入力に対して同じ出力が得られるかの保証

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[RAG（検索拡張生成）]]
- [[プロンプトエンジニアリング]]
`,
    ["AI", "エージェント", "2026"]
  );

  // ─── 3. RAG ─────────────────────────────────────────────────────────────────
  await createPage(
    "rag-retrieval-augmented-generation",
    "RAG（検索拡張生成）",
    `# RAG（検索拡張生成）

RAG（Retrieval-Augmented Generation）は、LLMの回答生成時に外部の知識ベースを動的に検索して文脈として注入する手法。モデルの学習データにない最新情報や社内固有の知識を扱えるようになる。

## 基本的な仕組み

\`\`\`
質問
 ↓
ベクトル検索（クエリを埋め込みに変換して類似文書を検索）
 ↓
関連チャンクを取得
 ↓
LLMにプロンプト＋チャンクを渡す
 ↓
回答生成（ソース引用付き）
\`\`\`

## RAGの進化系（2025〜2026年）

### Agentic RAG（エージェント型RAG）
従来の固定的な「1回検索して終わり」から、自律的エージェントが複数回検索・計画・反省を行う**動的パイプライン**へ進化。

- ベースラインより一貫して **5〜13ポイントの精度向上**
- **A-RAG**（2026年2月発表）：階層的検索インターフェースでキーワード・セマンティック・チャンク単位の検索ツールを提供

### GraphRAG（グラフ型RAG）
コーパス上にエンティティ関係グラフを構築し、「このドキュメント全体に共通するテーマは？」といった**グローバルな問い**に対応できるようにする手法。

- Microsoftのオープンソース実装が広く使われる
- 2025年にメインストリームのRAGパターンとして急速に普及

### Multimodal RAG（マルチモーダルRAG）
テキストだけでなく、画像・動画・音声も検索対象とするRAG。フロンティアモデルのマルチモーダル化に伴い実用化が進んだ。

### Self-RAG
いつ検索するかを自己判断し、自分の出力を自己批評するよう訓練されたモデルを使うアプローチ。事実性と引用精度を向上させる。

## 実績事例

**MARAUS**（大学入試カウンセリング用マルチエージェントRAGシステム）
- 精度92%達成
- 幻覚率を15% → **1.45%** に削減

## RAGの概念的拡張

2025年のRAGFlowレポートでは、RAGは「検索拡張生成という特定パターン」から「インテリジェント検索をコアとするコンテキストエンジン」へと概念的に拡張されたと評価されている。

## このWikiでのRAG実装

このWikiは Azure AI Search でベクトルインデックスを構築しており、チャット機能でRAGを実現している。

1. 質問をクエリとしてAzure AI Searchに送信
2. セマンティック検索で関連ページを取得
3. Claude Sonnet 4.6 に文脈として渡す
4. ソースリンク付きで回答を生成

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[AIエージェント]]
`,
    ["AI", "RAG", "LLM", "2026"]
  );

  // ─── 4. プロンプトエンジニアリング ────────────────────────────────────────
  await createPage(
    "prompt-engineering",
    "プロンプトエンジニアリング",
    `# プロンプトエンジニアリング

プロンプトエンジニアリングとは、LLMから望ましい出力を得るためのプロンプト（入力テキスト）の設計・最適化技術。モデルの特性を理解して適切な指示を与えることで、精度・一貫性・安全性を大幅に向上させられる。

## 完了モデル vs 推論モデル（2025年の大転換）

2025年に「推論モデル」が普及し、プロンプトの書き方を根本的に見直す必要が出てきた。

| 観点 | 完了モデル（GPT-4.1等） | 推論モデル（o3・o4・Claude Thinking等） |
|---|---|---|
| プロンプトスタイル | ステップバイステップで誘導する | 「何をすべきか」だけ明示、方法は任せる |
| CoT指示 | 有効（「ステップで考えて」が効く） | 効果薄・むしろ邪魔になる場合あり |
| 比喩 | 逐一指示が必要な新人 | 自分でアプローチを決める上級同僚 |
| 詳細な手順指定 | 有効 | 自律性を阻害する可能性あり |

## 主要テクニック

### Chain-of-Thought（CoT）
複雑な問題を段階的なステップに分解させる。「ステップで考えてください」と指示するだけで推論精度が向上する。1000億パラメータ以上のモデルで特に効果的。

\`\`\`
悪い例: この問題の答えは？
良い例: この問題をステップごとに考えてください。まず...
\`\`\`

### Tree of Thoughts（ToT）
CoTの拡張。複数の推論パスを木構造で同時に生成・探索する。戦略的計画が必要な問題に強い。

### Self-Consistency
同じ問いに対して複数の推論パスを生成し、最も一貫した答えを多数決で選択。ランダムエラーを低減する。

### Role Prompting
「あなたは○○のエキスパートです」という役割設定。精度・推論・関連性が即座に向上する。

\`\`\`
例: あなたはセキュリティ専門家です。以下のコードを脆弱性の観点でレビューしてください。
\`\`\`

### Meta Prompting
プロンプト自体をAIが自動的に最適化・生成する手法。人間によるチューニングを最小化する方向に進化中。

### Constitutional AI
倫理的制約をプロンプトやシステムプロンプトに組み込む手法。Anthropicが提唱し、現在広く採用されている。

## エージェント向けプロンプト設計

複数ステップ・ツール使用・ループを前提とするエージェントでは、以下を意識する：

- **タスクの明確な境界定義**：何をすべきで何をすべきでないかを明示
- **ツールの使用条件を明記**：どんな時にどのツールを使うかのルールを与える
- **エラー時の振る舞い指定**：想定外の状況でどう対処するかを記述
- **終了条件の明示**：ループが無限に続かないよう完了基準を与える

## 2025〜2026年のトレンド

- **自動プロンプト最適化**：人間が設計する時代からAI自身がプロンプトを最適化する時代へ
- **マルチモーダルプロンプト**：テキスト・画像・音声を組み合わせたプロンプト設計が標準化
- **ファインチューニングとの使い分け**：繰り返し使うタスクはファインチューニング、柔軟性が必要な場合はプロンプトという棲み分け

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[AIエージェント]]
`,
    ["AI", "プロンプト", "LLM", "2026"]
  );

  // ─── 5. マルチモーダルAI ───────────────────────────────────────────────────
  await createPage(
    "multimodal-ai",
    "マルチモーダルAI",
    `# マルチモーダルAI

マルチモーダルAIとは、テキストだけでなく画像・音声・動画・コードなど複数の「モダリティ（情報形式）」を理解・生成できるAIモデル。2025〜2026年にフロンティアモデル全てがマルチモーダルを標準搭載し、業界の前提が変わった。

## 主要モデルとマルチモーダル能力（2026年）

### Gemini 3.1 Pro（Google）— マルチモーダル最強
- テキスト・画像・音声・動画・コードを**単一の会話でネイティブ処理**
- コンテキスト200万トークン（2時間の動画を一括入力可能）
- 16ベンチマーク中13項目でトップ（GPQA Diamond: 94.3%、ARC-AGI-2: 77.1%）
- 2026年2月19日リリース

### GPT-5.4（OpenAI）
- テキスト・画像・音声のネイティブ処理（前処理ステップ不要）
- 音声応答**320ミリ秒**のリアルタイム音声会話
- コンピューターユース（OSWorld 75%）で人間専門家水準を超過
- 2026年3月ロールアウト

### Claude Opus 4.6（Anthropic）
- ビジョン対応（画像・ドキュメント解析）
- コンテキスト100万トークン
- コーディングとドキュメント処理の組み合わせに強み

### DeepSeek V4（オープンウェイト）
- テキスト・画像・音声・**動画**のネイティブマルチモーダル
- MODEL1アーキテクチャでメモリ40%削減・推論1.8倍高速
- 2026年3月3日リリース

### Llama 4（Meta / オープンウェイト）
- Maverick・Behemothがマルチモーダル対応
- ScoutはテキストとコードにフォーカスしたGPUフレンドリー設計

## アーキテクチャの進化

### ネイティブマルチモーダル統合
初期のマルチモーダルモデルは「テキストモデル＋ビジョンアダプター」という構造だったが、現在は**統一された内部表現**で全モダリティを処理するアーキテクチャが主流。モダリティをまたいだ推論の精度が向上した。

### 超長コンテキストとマルチモーダルの融合
長大なコンテキストウィンドウとマルチモーダルが組み合わさることで：
- 会議の録画全体（音声＋スライド）を一括で要約
- 大規模コードベース＋設計図を同時に参照したコードレビュー
- 数十本の研究論文（テキスト＋図表）を横断した文献調査

## 実用ユースケース

| ユースケース | 使用モダリティ | 活用モデル例 |
|---|---|---|
| ドキュメント解析（PDF・スライド） | テキスト＋画像 | Claude Opus 4.6 / GPT-5.4 |
| リアルタイム音声アシスタント | 音声 | GPT-5.4 |
| 動画コンテンツの要約・検索 | 動画＋テキスト | Gemini 3.1 Pro |
| コンピューター操作の自動化 | 画面キャプチャ＋テキスト | GPT-5.4 |
| 医療画像診断支援 | 画像＋テキスト | Gemini 3.1 Pro |
| グラフ・チャートの解析 | 画像→テキスト | Claude Opus 4.6 |

## オープンソースの急追

商用モデルのマルチモーダル独占に対し、オープン系モデルがキャッチアップ中：
- **Qwen3-VL**（Alibaba）：コスト効率とマルチモーダル性能のバランス
- **GLM-4.6V**（Zhipu AI）：NVIDIA非依存でフロンティアモデルに肉薄
- **DeepSeek V4**：ネイティブマルチモーダルでフルオープンウェイト

## 関連ページ

- [[LLM（大規模言語モデル）概要]]
- [[RAG（検索拡張生成）]]
`,
    ["AI", "マルチモーダル", "LLM", "2026"]
  );

  console.log("\n✓ AI Wiki ページ作成完了");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
